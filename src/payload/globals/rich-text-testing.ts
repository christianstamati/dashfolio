import type { GlobalConfig } from "payload";
import { richText } from "@/payload/fields/rich-text";

export const RichTextTesting: GlobalConfig = {
  slug: "richTextTesting",
  fields: [richText],
};
